let num1 = 12
let num2 = 12

console.log(num1 + num2)


// for run file in nodejs
// node ./index.js

// Either we use the above method to run the file,
// or we use the packaje.json file to enter a container in the script section,
// which will run by entering that file.
