var num1 = 12;
var num2 = 3;
console.log(num1 + num2);
/*In order for our file to run, it must be translated into a JavaScript file.
 For this, we use the following method*/
// npm install typescript --save-dev
// tsc app.ts
/*We use the following command so that there is no need for translation
 by creating tfirats in the file*/
// tsc app.ts --watch
// tsc app.ts -w
