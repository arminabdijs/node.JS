//The letter g means global
//flag / /g

// If we don't do anything, it will only search the first line
//flag / /

//We can use several flags at the same time
//flag / /gim

//flag i means not case sensitive
//flag / /gi


//flag m means multiline, it means that ^ and $ will match the start and end of each line
//          /^([A-Z])\w+/m


//flag u means unicode, it means that \w will match any unicode character


//flag y means sticky, it means that ^ and $ will match the start and end of the last match


//flag s means single line, it means that \n will match \r and \n
